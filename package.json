{
  "name": "orion-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "ci:backend": "cd backend && npm ci && npm run lint && npm run build && npm test",
    "ci:frontend": "cd frontend && npm ci && npm run lint && npm run build && npm test",
    "ci": "npm run ci:backend && npm run ci:frontend",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^14.0.0"
  },
  "lint-staged": {
    "backend/src/**/*.{ts,js}": [
      "npm --prefix backend run lint:fix"
    ],
    "frontend/src/**/*.{ts,tsx,js,jsx}": [
      "npm --prefix frontend run lint:fix",
      "npm --prefix frontend run format:fix"
    ]
  }
}
